name: Plugin Check

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_call:

jobs:
  plugin-check:
    name: WordPress Plugin Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.1'
          tools: composer
          coverage: none

      - name: Install npm dependencies (root) and build
        run: |
          npm ci
          npm run build

      - name: Install npm dependencies (block) and build
        working-directory: Block/archives-widget-extended
        run: |
          npm ci
          npm run build

      - name: Install Composer runtime dependencies
        run: composer install --no-dev --optimize-autoloader --prefer-dist --no-progress --no-interaction

      - name: Assemble release artifact
        run: |
          set -euo pipefail
          mkdir -p build/archives-widget-extended
          rsync -a \
            --include='/Block/' \
            --include='/Block/archives-widget-extended/' \
            --include='/Block/archives-widget-extended/build/***' \
            --exclude='/Block/archives-widget-extended/*' \
            --exclude='/.distignore' \
            --exclude='/.git' \
            --exclude='/.github' \
            --exclude='/.claude' \
            --exclude='/.editorconfig' \
            --exclude='/.gitignore' \
            --exclude='/.nvmrc' \
            --exclude='/.phpcs-cache' \
            --exclude='/.phpcs.xml.dist' \
            --exclude='/.wp-env.json' \
            --exclude='/composer.lock' \
            --exclude='/package.json' \
            --exclude='/package-lock.json' \
            --exclude='/phpcs.xml' \
            --exclude='/phpunit.xml.dist' \
            --exclude='/postcss.config.js' \
            --exclude='/vite.config.js' \
            --exclude='/tests' \
            --exclude='/assets' \
            --include='/media/' \
            --include='/media/plugin/***' \
            --exclude='/media/*' \
            --exclude='/CHANGELOG.md' \
            --exclude='/CLAUDE.md' \
            --exclude='/INTEGRATION-*.md' \
            --exclude='/README.md' \
            --exclude='/build' \
            --exclude='node_modules' \
            ./ build/archives-widget-extended/

      - name: List assembled artifact
        run: find build/archives-widget-extended -maxdepth 3 -print

      - name: Run Plugin Check
        uses: wordpress/plugin-check-action@v1
        with:
          build-dir: build/archives-widget-extended

      - name: Upload release artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: archives-widget-extended-release
          path: build/archives-widget-extended
          retention-days: 7
